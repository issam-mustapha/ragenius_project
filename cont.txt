1-- structure le projet app..
2-- creer dockerfile selon votre besoins
3-- docker-compose.yml definir les services que tu veux
4-- creer requirmenet.txt
5-- ajouter docker-ignore et env
6-- tu va lancer image et container en meme temps avec 

docker-compose up --build

7-- tu va avoir les roots
http://localhost:8000/docs#/
http://localhost:8000/  cet route que tu va definir dans main principale /
ou ce url 
http://127.0.0.1:8000/


===============maintenant tous fonctionne bien alors passer au venv pour coder localement ==============
8-- active virtual machine avec 
python -m venv venv
9-- entrer au venv avec cet commande
venv\Scripts\activate
10-- installer les packages sur virtual machine aussi avec cet commande
pip install -r requirements.txt
============================== un autre solution Dev container pas appele de virtual machine tous est dans Dev container===============

pour start 
ctrl+shift+p==> 

Dev Container rebuild in


pour arreter container via vs code 
1- clique en bas à gauche
2- Dev Container: FastAPI Dev
3-
   Puis :
   Clique dessus
   Une palette s’ouvre
   Choisis :Reopen Folder Locally
   ===========> Cela va fermer le container et revenir à ton environnement local.



   ===========================git and github==============
telecharger git in container avec ces commandes
apt update
apt install -y git

verifie avec 
git --version

apres ajouter .gitignore
puis cache ce que tu veut cacher 


====================grapha + prometheus==============
simple tu ajouter graphana dans docker compose 
apres ouvrir ce site local


http://localhost:3000
apres mettre dans datasource
http://prometheus:9090
tu va avoir les graphics de site comment fonctionne...


=========================== poetry ============================

PS D:\my work space here\test_docker> poetry --version
Poetry (version 2.3.2)
PS D:\my work space here\test_docker> poetry init

This command will guide you through creating your pyproject.toml config.

Package name [test_docker]:
Version [0.1.0]:  
Description []:  test docker and livrer the projects
Author [issam-mustapha <issamadoch777@gmail.com>, n to skip]:  issam adoch
License []:  
Compatible Python versions [>=3.11]:  

Would you like to define your main dependencies interactively? (yes/no) [yes] no
Would you like to define your development dependencies interactively? (yes/no) [yes] no
Generated file

[project]
name = "test-docker"
version = "0.1.0"
description = "test docker and livrer the projects"
authors = [
    {name = "issam adoch"}
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"


Do you confirm generation? (yes/no) [yes] yes
PS D:\my work space here\test_docker>
apres ca tu va ajouter des dependencies que tu veux.. tous va installer avec des version compatible et pro 
====================================================fin de cmd ==================






tu sauvgarder les dependance dans le container
pip freeze > requirements.txt


tester que si container est lier avec github via la commande
ssh -T git@github.com
verfiie les cle via 
ssh -T git@github.com
verifie le reseau
ping github.com

============================pour voir les logs dans Dev container ================
avec dubuggy------------------------------------------------
poetry run python -Xfrozen_modules=off -m debugpy \
  --listen 0.0.0.0:5678 --wait-for-client \
  -m uvicorn app.main:app \
  --reload \
  --reload-dir /app \
  --host 0.0.0.0 \
  --port 8001 \
  --log-level debug
  vois logs en temps reel-------------------
  poetry run uvicorn app.main:app --host 0.0.0.0 --port 8001 --reload --reload-dir /app --log-level debug


  pour acceder a la base de donnes sur le container 
  docker exec -it <nom de container de base de donnees> psql -U <user ou root ou username> -d <nom de la base de donne>
  ca vous permettre d'execute des commandes..